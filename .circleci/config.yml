version: 2
jobs:
  build:
    docker:
    - image: circleci/node:8.10
    working_directory: ~/singnet/token-contracts
    steps:
    - checkout
    - run:
        name: Build
        command: |
          npm install
    - run:
        name: Test
        command: |
          npm run test
